{
  "name": "deltachat-node",
  "version": "1.0.0-beta.26",
  "description": "node.js bindings for deltachat-core",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "coverage-html-report": "rm -rf coverage/ && nyc report --reporter=html && opn coverage/index.html",
    "generate-constants": "./scripts/generate-constants.js",
    "install": "tsc && node-gyp-build \"node scripts/rebuild-core.js\" \"node scripts/postinstall.js\"",
    "rebuild-all": "npm run rebuild-core && npm run rebuild-bindings && tsc",
    "rebuild-core": "node scripts/rebuild-core.js",
    "rebuild-bindings": "npx node-gyp rebuild && npm run generate-constants",
    "build-typescript": "tsc",
    "prebuild": "prebuildify -t 10.16.0 --napi --strip --postinstall \"node scripts/postinstall.js --prebuild\"",
    "download-prebuilds": "prebuildify-ci download",
    "submodule": "git submodule update --recursive --init",
    "test": "standard && nyc node test/index.js",
    "test-integration": "node test/integration.js",
    "hallmark": "hallmark --fix"
  },
  "homepage": "https://github.com/deltachat/deltachat-node",
  "repository": {
    "type": "git",
    "url": "https://github.com/deltachat/deltachat-node.git"
  },
  "engines": {
    "node": ">=10.6.0"
  },
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "debug": "^4.1.1",
    "lodash.pick": "^4.4.0",
    "mkdirp": "^0.5.1",
    "napi-macros": "^2.0.0",
    "node-gyp-build": "^4.1.0",
    "typescript": "^3.7.5"
  },
  "devDependencies": {
    "@types/node": "^13.7.0",
    "coveralls": "^3.0.3",
    "hallmark": "^2.0.0",
    "node-gyp": "^6.0.0",
    "nyc": "^15.0.0",
    "opn-cli": "^5.0.0",
    "prebuildify": "^3.0.0",
    "prebuildify-ci": "^1.0.4",
    "split2": "^3.1.1",
    "standard": "^14.3.1",
    "tape": "^4.10.1",
    "tempy": "^0.3.0"
  }
}
