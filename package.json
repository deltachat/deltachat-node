{
  "name": "deltachat-node-napi",
  "version": "0.0.0",
  "description": "node.js bindings for deltachat-core based on napi",
  "scripts": {
    "start": "node-gyp rebuild && node example.js",
    "dev": "node-gyp rebuild --debug --verbose",
    "preinstall": "git submodule update --recursive --init",
    "install": "./scripts/rebuild-all",
    "test": "standard"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Jikstra/deltachat-node-napi.git"
  },
  "engines": {
    "node": ">=8.4.0"
  },
  "license": "GPL-3.0",
  "dependencies": {
    "napi-macros": "ralphtheninja/napi-macros#check-status",
    "node-gyp-build": "^3.4.0"
  },
  "devDependencies": {
    "standard": "^11.0.1"
  }
}
