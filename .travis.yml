language: node_js

os:
  - osx

before_install:
  - ./scripts/travis-before-install

node_js:
  - 8

install:
  - npm install --verbose

script:
  - npm test
  - npm run test-integration

notifications:
  email: false