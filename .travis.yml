language: node_js

os:
  - osx

node_js:
  - 8.6.0

before_install:
  - ./scripts/travis-before-install

install:
  - npm install --verbose

script:
  - npm test
  - npm run test-integration

# TODO add prebuildify and deploy stuff once
# integration tests pass

notifications:
  email: false